
    
    https://codepen.io/danyang-li/pen/powyJYa?editors=1000
    

    1. What is Semantic HTML? 
    Using HTML markup to reinforce the semantics, or meaning of the information in webpages and web applications. 

    2. What is HTML used for? 
    It is the markup language for describing the structure of web pages.

    3. What is an attribute and where do we put it? 
    Special words defining additional characteristics, behaviour,or properties of the elements.
    An attribute is added to an HTML opening tag of an element, after the element's name, appearing as name-value paires, separated by = .
    <element attribute="value">element content</element>

    4. What is the h1 tag used for? How many times should I use it on a page?
    Taging the sentence as the most important header.
    One time.

    5. Name two tags that have required attributes
    <img src="" alt="">
    <a href=" ></a>

    6. What do we put in the head of our HTML document?
    The information that we don't want to display on the webpage, such as document title, links to CSS, metadata information, styles, character set, etc.

    7. What is an id? 
    The HTML id specifies a unique identifier in the whole document. The value must be unique within the HTML document, just like driver's license number.
    
    8. What elements can I add an id to? 
    The elements of linking, scripting or styling, such as div, p, span,etc.

    9. How many times can I use the same id on a page? 
    one.

    10. What is a class? 
    The class is an attribute which specifies one or more class names for an element, like classroom.

    11. What elements can I add a class to? 
    Linking, scripting, styling, etc, such as div, span, p, etc.

    12. How many times can I use the same class on a page?
    unlimited 
    
    13. How do I get my link to open in a new tab?
    <a href="" target="_blank"></a>

    14. What is the alt attribute in the image tag used for? 
    Providing alternative information for an image if user cannot view it because of bad displaying.

    15. How do I reference an id?
    Using #

    16. What is the difference between a section and a div
    Section is HTML5 semantic element.
    Div has no semantic meaning. 

    17. What is CSS used for? 
    Cascading style sheets, used for describing the presentation of a document written in a markup language such as HTML.

    18. How to we select an element? Example - every h2 on the page
    h2 {

    }

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
    An id is the unique identifier in the whole document. We use the same id value only once on a page.
    A class is an attribute which specifies one or multiple elements that appear on the page for once or more than once.
    
    id: header, body, footer
    class: links


    20. How do we select classes in CSS?
    Using period(.) 
      .classname{

    }
    21. How do we select a p element with a single class of "human""?
    .human{

    }

    22. What is a parent child selector? When would this be useful? 
    For example, the selector selects all <p> element that are a direct child of its parent <div> element, does not select other <p> element whose are not a child element of <div> parent. 
    When we don't want to select all the child element, we only focus on the direct child element.

    23. How do you select all links within a div with the class of sidebar?
    .sidebar a:link {

    }

    24. What is a pseudo selector?
    Defining the state of an element, targeting the elements that can’t be targeted with combinators or simple selectors like id, class or tag type.
    selector:pseudo-class{
        property:value;
    }

    25. What do we use the change the spacing between lines?
    line-height 

    26. What do we use to change the spacing between letters?
    word-spacing

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    text-transform: uppercase;
    text-transform: lowercase;
    text-transform: capitalize;

    28. How do I add a 1px border around my div that is dotted and black?
       div{
           border: 1px dotted black;
    }

    29. How do I select everything on the page?
       *{

       }

    30. How do I write a comment in CSS?
       /* Comments here */

    31. How do I find out what file I am in, when I am using the command line? 
       pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
       ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
       rm filename.txt
       Because a file will be removed irrevocablely and permenantly.

    34. Why should I use version control? 
    To keep track of collaborative and personla projects,for example, help me to easily explore the changes I have made, maintain online backup, assign and review code by others, etc.

    35. How often should I commit to github?
    At least a couple of times every hour, 20 minutes recommanded.

    36. What is the command we would use to push our repo up to github? 
    git push -u origin "branch-name"

    37. Walk me through Lambda's git flow. 
    fork on Github(upper right)
    name should be in front of the repo name before slash
    copy clone SSH link
    open terminal or Gitbash
    use cd command to go to the directory where you want to clone your forked repo
    git clone <link-to-repo> command
    enter SSH passphrase
    cd <repo-name> cd into newly created repo
    git checkout -b "firstname-lastname"
    code . to open current repo in VSCode from terminla or Gitbash
    Do work and save as many times as you can.
    git add . will add repo changes, makes them ready to commit
    git commit -m "comment content"
    git push -u origin "firstname-lastname"
        git push after the first push
    enter SSH key
    go to repo on Github
    select firstname-lastname instead of master from the dropdown menu top left 
    Leave a comment if you wish
    do not merge request
    create pull request
    copy URL and submit



Stretch Questions

    1. What is the difference between an inline element and a block element?
    A block element starts on a new line and takes up the full width available.It has a top and a bottom margin.
    An inline element does not start on a new line, only takes up as much width as necessary, and don't have a margin.

    2. What happens when an element is positioned absolutely? 
    It will position itself relative to the nearest positioned ancestor.


    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    overflow:hidden;

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    diplay block by default: div h1-h6 ul
    display inline by default: span link
    disply inline-block by default: button

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?